export const ThemeBlocksCard = ({
  blockAdded,
  viewStoreUrl,
  editThemeUrl,
  addBlockUrl,
}) => {
  return (
    <s-section>
      <s-stack direction="block" gap="small">
        <s-heading>Theme Blocks</s-heading>
        <s-description>
          Add the Instagram feed block to your store pages.
        </s-description>
      </s-stack>
    </s-section>
  );
};
